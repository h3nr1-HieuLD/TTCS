apiVersion: v1
kind: Service
metadata:
  name: ttcs-backend
  labels:
    app: ttcs-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ttcs-backend